=== PARTIAL SUMMARIES ===

{parts}

=== INSTRUCTIONS ===

The text above contains several partial summaries of the same research paper, each covering a different portion of the paper. Merge them into ONE complete, concise, structured summary of the entire paper, based solely on the content provided.

Organize the final summary into the following sections using plain text only:

1. Problem / Motivation — What problem does the paper address?
2. Method / Approach — What method or approach is proposed?
3. Key Results — What are the main findings or results?
4. Conclusion — What conclusions do the authors draw?

CRITICAL DEDUPLICATION RULES:
- If two or more partial summaries state the same fact, finding, or contribution (even if worded differently), state it ONCE. Do not repeat it in multiple sections or multiple lines.
- When the same claim appears with slightly different wording, merge them into a single, consistent sentence rather than listing both versions.
- Keep every distinct fact from the partial summaries. Deduplication must not drop information — only merge identical or near-identical claims.

CRITICAL CONTRADICTION RULES:
- If two partial summaries directly contradict each other (e.g., one says the method is X, another says it is Y), do NOT silently pick one. State the disagreement explicitly in the relevant section, for example: "The paper reports conflicting descriptions of the method (one part states X, another states Y)."
- If the contradiction is only apparent because the claims are unrelated or address different aspects, do not force them together; present each claim as its own distinct point.

If a partial summary says a section was not determinable from its portion, try to fill it from other parts if possible; otherwise state that explicitly.

CRITICAL: Start each of the 4 sections on a new line with a blank line between sections. Put each distinct finding on its own line. Never write in dense paragraphs. Keep each section brief (2-4 sentences).

Do NOT use any markdown formatting in your response. No asterisks, hashes, dashes, or markdown symbols. Use plain text with numbered sections and colons for labels.
